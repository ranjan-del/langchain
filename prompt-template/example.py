"""
Prompt template — LangChain example (scaffold).

This is placeholder/boilerplate code only. See MEMORY.md checklist item:
"Each folder: minimal runnable example + short README".

TODO: Implement the Prompt template example. Nothing here is wired up yet.
"""

import os

# TODO: load environment variables (API keys) from a .env file
# from dotenv import load_dotenv
# load_dotenv()


def main() -> None:
    """Entry point for the Prompt template example."""
    # TODO: construct a PromptTemplate / ChatPromptTemplate and format it with variables
    print("[prompt-template] scaffold — not yet implemented. See README.md and MEMORY.md")


if __name__ == "__main__":
    main()
